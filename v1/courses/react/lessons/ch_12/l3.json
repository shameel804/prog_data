{
    "id": "l3",
    "chapterId": "ch_12",
    "title": "Defining Routes with Routes and Route",
    "description": "Map URLs to components.",
    "xpReward": 20,
    "estimatedMinutes": 6,
    "section": "Core",
    "slides": [
        {
            "id": "s_l3_1",
            "type": "text",
            "title": "Routes and Route Components 🗺️",
            "content": "# Setting Up Routes\n\nAfter wrapping your app with `BrowserRouter`, you define your routes using the `Routes` and `Route` components.\n\n* `Routes` looks through its children `Route` elements and renders the first one whose path matches the current URL.\n* `Route` has a `path` prop and an `element` prop (the component to render).\n\n```jsx\nimport { Routes, Route } from 'react-router-dom';\nimport Home from './Home';\nimport About from './About';\n\nfunction App() {\n  return (\n    <BrowserRouter>\n      <Routes>\n        <Route path=\"/\" element={<Home />} />\n        <Route path=\"/about\" element={<About />} />\n      </Routes>\n    </BrowserRouter>\n  );\n}\n```\n\nNow, when the URL is `/`, the `Home` component renders; when it's `/about`, `About` renders."
        },
        {
            "id": "s_l3_2",
            "type": "code",
            "title": "Example with Multiple Routes",
            "content": "Add a 404 (not found) route using a path of `*`.",
            "codeSnippet": "import { Routes, Route } from 'react-router-dom';\n\nfunction App() {\n  return (\n    <Routes>\n      <Route path=\"/\" element={<Home />} />\n      <Route path=\"/about\" element={<About />} />\n      <Route path=\"/contact\" element={<Contact />} />\n      <Route path=\"*\" element={<NotFound />} />\n    </Routes>\n  );\n}",
            "codeLanguage": "jsx",
            "canRunCode": false
        },
        {
            "id": "s_l3_3",
            "type": "quiz",
            "title": "Quick Check 🧠",
            "content": "",
            "quizData": {
                "id": "q_l3_1",
                "type": "true_false",
                "question": "The `Routes` component renders all matching `Route` components.",
                "correctAnswer": false,
                "explanation": "False. `Routes` renders only the first matching `Route`, not all matches."
            }
        }
    ]
}