{
    "id": "task2",
    "chapterId": "ch_3",
    "title": "Logical AND",
    "description": "Use the && operator to check if multiple conditions are met simultaneously.",
    "estimatedMinutes": 5,
    "problemStatement": "Check two conditions at once. Given `let age = 25` and `let hasLicense = true`, use the `&&` operator to print `true` only if both are truthy. (Represent the logic: age is >= 18 AND has license).\n\n**Requirement:** Print the result of the boolean expression directly.",
    "hints": [
        "Example: `console.log(x > 5 && y < 10);`"
    ],
    "expectedOutput": "true",
    "solutionCode": "let age = 25;\nlet hasLicense = true;\nconsole.log(age >= 18 && hasLicense);"
}
