{
    "id": "task1",
    "chapterId": "ch_6",
    "title": "Class Init",
    "description": "Create a class with an init block for logic that runs during object creation.",
    "estimatedMinutes": 5,
    "problemStatement": "Object initialization is important. Create a class `User` that takes a `name: String`. Use an `init` block to print `\"User $name created\"` when an object is instantiated.\n\n**Expected Output:** `User Bob created`.",
    "hints": [
        "The `init` block runs immediately after the primary constructor."
    ],
    "expectedOutput": "User Bob created",
    "solutionCode": "class User(name: String) {\n    init {\n        println(\"User $name created\")\n    }\n}\n\nfun main() {\n    val user = User(\"Bob\")\n}"
}
