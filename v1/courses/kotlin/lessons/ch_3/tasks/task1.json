{
    "id": "task1",
    "chapterId": "ch_3",
    "title": "When Expression",
    "description": "Use the when expression to categorize a number value.",
    "estimatedMinutes": 10,
    "problemStatement": "Kotlin's `when` is like `switch` on steroids. Given `val x = 2`, use `when` to print `\"One\"` if x is 1, `\"Two\"` if x is 2, and `\"Other\"` otherwise.\n\n**Requirement:** Include the `else` case.",
    "hints": [
        "Syntax: `when(x) { 1 -> println(\"...\") ... }`."
    ],
    "expectedOutput": "Two",
    "solutionCode": "fun main() {\n    val x = 2\n    when (x) {\n        1 -> println(\"One\")\n        2 -> println(\"Two\")\n        else -> println(\"Other\")\n    }\n}"
}
