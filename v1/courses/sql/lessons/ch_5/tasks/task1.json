{
    "id": "task1",
    "chapterId": "ch_5",
    "title": "Count Rows",
    "description": "Use the COUNT() aggregate function to determine the total number of records in a dataset.",
    "estimatedMinutes": 5,
    "problemStatement": "Total volume! Write a query that returns the total count of all rows in the `orders` table.\n\n**Requirement:** Use the `COUNT(*)` function.",
    "hints": [
        "Aggregate functions like `COUNT` condense many rows into a single summary value."
    ],
    "expectedOutput": "A single number representing the total orders",
    "solutionCode": "SELECT COUNT(*) FROM orders;"
}
