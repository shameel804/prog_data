{
    "id": "task1",
    "chapterId": "ch_3",
    "title": "Where Clause",
    "description": "Filter rows from a table based on a simple numerical condition using the WHERE clause.",
    "estimatedMinutes": 5,
    "problemStatement": "Narrow it down! Find all records from the `inventory` table where the `quantity` is exactly `0`.\n\n**Requirement:** Use the `WHERE` keyword.",
    "hints": [
        "The `=` sign is used for equality checks in SQL."
    ],
    "expectedOutput": "All rows with 0 quantity",
    "solutionCode": "SELECT * FROM inventory WHERE quantity = 0;"
}
