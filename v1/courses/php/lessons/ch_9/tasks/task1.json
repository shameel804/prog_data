{
    "id": "task1",
    "chapterId": "ch_9",
    "title": "GET Request",
    "description": "Access data passed in the URL query string using $_GET.",
    "estimatedMinutes": 5,
    "problemStatement": "Read the URL! If a user visits `test.php?id=5`, write the code to print the value of `id` from the `$_GET` superglobal.\n\n**Requirement:** Use `isset()` to avoid errors if `id` is missing.",
    "hints": [
        "Superglobals are automatically available everywhere."
    ],
    "expectedOutput": "5",
    "solutionCode": "<?php\nif (isset($_GET['id'])) {\n    echo $_GET['id'];\n}\n?>"
}
