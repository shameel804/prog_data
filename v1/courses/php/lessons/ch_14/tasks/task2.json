{
    "id": "task2",
    "chapterId": "ch_14",
    "title": "Execute Query",
    "description": "Run a simple SELECT query using a PDO prepared statement.",
    "estimatedMinutes": 10,
    "problemStatement": "Query data safely! Given `$pdo`, prepare a statement `SELECT * FROM users WHERE id = ?`. Bind the value `1` to the placeholder and execute the query.\n\n**Requirement:** Use `prepare()` and `execute()`.",
    "hints": [
        "Prepared statements prevent SQL injection."
    ],
    "expectedOutput": "The query is executed with the bound value",
    "solutionCode": "<?php\n$stmt = $pdo->prepare(\"SELECT * FROM users WHERE id = ?\");\n$stmt->execute([1]);\n?>"
}
