{
    "id": "l3",
    "chapterId": "ch_14",
    "title": "Querying Data: SELECT",
    "description": "Retrieve data using query() and fetch().",
    "xpReward": 25,
    "estimatedMinutes": 7,
    "section": "Querying",
    "slides": [
        {
            "id": "s_l3_1",
            "type": "text",
            "title": "PDO::query()",
            "content": "# Simple SELECT with query()\n\nIf your query has no user input, you can use `query()` to execute it directly. It returns a `PDOStatement` object.\n\n```php\n$stmt = $pdo->query('SELECT * FROM users');\n```\n\nThen you can fetch rows with `fetch()` or `fetchAll()`."
        },
        {
            "id": "s_l3_2",
            "type": "code",
            "title": "Fetching Data",
            "content": "```php\n<?php\n$stmt = $pdo->query('SELECT id, name, email FROM users');\n\n// Fetch one row at a time\nwhile ($row = $stmt->fetch()) {\n    echo $row['name'] . ' - ' . $row['email'] . \"\\n\";\n}\n\n// Or fetch all at once\n$users = $stmt->fetchAll();\nforeach ($users as $user) {\n    echo $user['name'] . \"\\n\";\n}\n?>\n```\n\n`fetch()` returns `false` when no more rows are available.",
            "codeSnippet": "<?php\n$stmt = $pdo->query('SELECT id, name, email FROM users');\nwhile ($row = $stmt->fetch()) {\n    echo $row['name'] . ' - ' . $row['email'] . \"\\n\";\n}\n?>",
            "codeLanguage": "php",
            "canRunCode": false
        },
        {
            "id": "s_l3_3",
            "type": "quiz",
            "title": "Quick Check 🧠",
            "content": "",
            "quizData": {
                "id": "q_l3_1",
                "type": "multiple_choice",
                "question": "What does PDO::query() return?",
                "options": [
                    {
                        "id": "opt1",
                        "text": "An array of rows",
                        "isCorrect": false
                    },
                    {
                        "id": "opt2",
                        "text": "A PDOStatement object",
                        "isCorrect": true
                    },
                    {
                        "id": "opt3",
                        "text": "A boolean",
                        "isCorrect": false
                    },
                    {
                        "id": "opt4",
                        "text": "The number of affected rows",
                        "isCorrect": false
                    }
                ],
                "explanation": "query() returns a PDOStatement object that you can use to fetch data.",
                "xpReward": 10
            }
        }
    ]
}