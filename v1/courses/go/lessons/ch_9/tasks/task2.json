{
    "id": "task2",
    "chapterId": "ch_9",
    "title": "Channel Communication",
    "description": "Send a string from a goroutine to the main thread via a channel.",
    "estimatedMinutes": 10,
    "problemStatement": "Channels are for communication and synchronization! Create a channel of strings using `make(chan string)`. In a goroutine, send the string `\"Ping\"` to the channel. In `main`, receive it and print it.\n\n**Requirement:** Use the `<-` operator for both sending and receiving.",
    "hints": [
        "Sending: `ch <- \"data\"` | Receiving: `msg := <- ch`."
    ],
    "expectedOutput": "Ping",
    "solutionCode": "package main\n\nimport \"fmt\"\n\nfunc main() {\n    ch := make(chan string)\n    go func() {\n        ch <- \"Ping\"\n    }()\n    \n    msg := <-ch\n    fmt.Println(msg)\n}"
}
