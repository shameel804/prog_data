{
    "id": "task2",
    "chapterId": "ch_7",
    "title": "Struct Initialization",
    "description": "Define a Person struct and initialize it with field names.",
    "estimatedMinutes": 5,
    "problemStatement": "Structs group data. Create a `User` struct with `Name` (string) and `Age` (int). Initialize a user named `\"Bob\"` with age `25` using the field name syntax. Print the struct.\n\n**Expected Output:** `{Bob 25}`.",
    "hints": [
        "Syntax: `u := User{Name: \"...\", Age: ...}`"
    ],
    "expectedOutput": "{Bob 25}",
    "solutionCode": "package main\n\nimport \"fmt\"\n\ntype User struct {\n    Name string\n    Age  int\n}\n\nfunc main() {\n    u := User{Name: \"Bob\", Age: 25}\n    fmt.Println(u)\n}"
}
