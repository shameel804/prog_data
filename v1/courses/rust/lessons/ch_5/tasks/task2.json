{
    "id": "task2",
    "chapterId": "ch_5",
    "title": "Reference Borrow",
    "description": "Pass a reference to a function to borrow data without taking ownership.",
    "estimatedMinutes": 5,
    "problemStatement": "Borrowing! Write a function `calculate_length` that takes a reference to a string `&String` and returns its length (`len()`).\n\n**Requirement:** The function should not take ownership of the string.",
    "hints": [
        "References are created using the ampersand `&` symbol."
    ],
    "expectedOutput": "Length of the string",
    "solutionCode": "fn main() {\n    let s1 = String::from(\"hello\");\n    let len = calculate_length(&s1);\n    println!(\"Length is {}\", len);\n}\n\nfn calculate_length(s: &String) -> usize {\n    s.len()\n}"
}
