{
    "id": "task2",
    "chapterId": "ch_4",
    "title": "Loop Label",
    "description": "Use loop labels to specify which loop to exit or continue when working with nested loops.",
    "estimatedMinutes": 5,
    "problemStatement": "Breaking labels! Label an outer loop `'outer`. Inside a nested loop, call `break 'outer;` when a certain condition is met.\n\n**Requirement:** The program should immediately terminate the outer loop.",
    "hints": [
        "Loop labels allow you to break from a specific outer loop instead of just the inner-most one."
    ],
    "expectedOutput": "Prints once and exits",
    "solutionCode": "fn main() {\n    'outer: loop {\n        loop {\n            println!(\"Looping...\");\n            break 'outer;\n        }\n    }\n}"
}
