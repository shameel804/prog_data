{
    "id": "task2",
    "chapterId": "ch_4",
    "title": "Return Values",
    "description": "Define functions that perform calculations and return result values to the caller.",
    "estimatedMinutes": 5,
    "problemStatement": "Square it! Write a function `square(number:)` that takes an `Int` and returns its square (number * number).\n\n**Requirement:** Use the `->` symbol to define the return type.",
    "hints": [
        "Include a `return` keyword unless the function is only one line (Implicit Returns)."
    ],
    "expectedOutput": "25",
    "solutionCode": "func square(number: Int) -> Int {\n    return number * number\n}\n\nlet result = square(number: 5)\nprint(result)"
}
