{
    "id": "task2",
    "chapterId": "ch_3",
    "title": "Switch Case Logic",
    "description": "Determine a color based on a category string using a switch statement.",
    "estimatedMinutes": 5,
    "problemStatement": "Write a function `Color getColor(String category)` that returns `Colors.blue` for `'work'`, `Colors.red` for `'urgent'`, and `Colors.grey` for anything else. Use a `switch` statement.\n\n**Requirement:** Print the color value for the input `'urgent'`.",
    "hints": [
        "Use `switch (category) { case 'work': ... }` syntax."
    ],
    "expectedOutput": "Color(0xfff44336) (Red)",
    "solutionCode": "import 'package:flutter/material.dart';\n\nColor getColor(String category) {\n  switch (category) {\n    case 'work': return Colors.blue;\n    case 'urgent': return Colors.red;\n    default: return Colors.grey;\n  }\n}\n\nvoid main() {\n  print(getColor('urgent'));\n}"
}
