{
    "id": "task2",
    "chapterId": "ch_11",
    "title": "Theme Persistence",
    "description": "Read a boolean value from local storage to determine if Dark Mode is enabled.",
    "estimatedMinutes": 10,
    "problemStatement": "Retrieve data! Write a function `Future<bool> isDarkMode()` that reads the boolean key `'darkMode'` from `SharedPreferences`.\n\n**Requirement:** Return `false` if the key does not exist (using the `??` operator).",
    "hints": [
        "Use `prefs.getBool('darkMode') ?? false`."
    ],
    "expectedOutput": "false (default result)",
    "solutionCode": "import 'package:shared_preferences/shared_preferences.dart';\n\nFuture<bool> isDarkMode() async {\n  final prefs = await SharedPreferences.getInstance();\n  return prefs.getBool('darkMode') ?? false;\n}"
}
