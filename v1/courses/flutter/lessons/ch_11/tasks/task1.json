{
    "id": "task1",
    "chapterId": "ch_11",
    "title": "SharedPreferences Save",
    "description": "Save a user's name locally using the SharedPreferences package.",
    "estimatedMinutes": 10,
    "problemStatement": "Persistence is key! Write an async function `saveName(String name)` that gets an instance of `SharedPreferences` and uses `.setString('username', name)` to save it.\n\n**Requirement:** Print `\"Saved!\"` when the operation completes.",
    "hints": [
        "Use `await SharedPreferences.getInstance()`."
    ],
    "expectedOutput": "Saved!",
    "solutionCode": "import 'package:shared_preferences/shared_preferences.dart';\n\nFuture<void> saveName(String name) async {\n  final prefs = await SharedPreferences.getInstance();\n  await prefs.setString('username', name);\n  print('Saved!');\n}"
}
