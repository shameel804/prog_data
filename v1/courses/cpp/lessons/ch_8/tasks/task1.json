{
    "id": "task1",
    "chapterId": "ch_8",
    "title": "Pointer Update",
    "description": "Change the value of a variable indirectly using a pointer.",
    "estimatedMinutes": 10,
    "problemStatement": "Pointers hold addresses. Define an `int score = 10` and an integer pointer `pScore` that points to it. Update `score` to `50` by only using `pScore`.\n\n**Expected Output:** `50`.",
    "hints": [
        "Use the dereference operator `*pScore = 50;`."
    ],
    "expectedOutput": "50",
    "solutionCode": "#include <iostream>\n\nint main() {\n    int score = 10;\n    int* pScore = &score;\n    *pScore = 50;\n    std::cout << score << std::endl;\n    return 0;\n}"
}
